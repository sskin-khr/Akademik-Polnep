@extends('layouts.dashboard')
@section('title', 'Kelola Struktur Organisasi')
@section('content')
<div class="min-h-screen bg-[#375766] p-4 sm:p-8"><div class="mx-auto max-w-5xl rounded-xl bg-white p-6 sm:p-8"><a href="{{ route('dashboard') }}" class="text-sm font-semibold text-sky-700">&larr; Kembali ke dashboard</a><h1 class="mt-5 font-outfit text-2xl font-extrabold text-slate-900">Konten Struktur Organisasi</h1><p class="mt-2 text-sm text-slate-500">Perbarui data dan foto anggota struktur organisasi.</p>@if (session('status'))<div class="mt-5 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('konten.struktur.update') }}" enctype="multipart/form-data" class="mt-7 space-y-4">@csrf@foreach ($items as $item)<div class="grid gap-3 rounded-lg border border-slate-200 p-4 md:grid-cols-4">@if ($item->photo)<img src="{{ asset($item->photo) }}" alt="{{ $item->name }}" class="h-20 w-20 rounded-lg object-cover">@endif<input name="items[{{ $item->id }}][name]" value="{{ $item->name }}" required class="rounded-lg border-slate-300 text-sm"><input name="items[{{ $item->id }}][jabatan]" value="{{ $item->jabatan }}" class="rounded-lg border-slate-300 text-sm"><div><input name="items[{{ $item->id }}][nip]" value="{{ $item->nip }}" class="w-full rounded-lg border-slate-300 text-sm"><input type="file" name="items[{{ $item->id }}][photo]" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full text-xs"></div></div>@endforeach<button class="rounded-lg bg-sky-600 px-5 py-3 text-sm font-bold text-white hover:bg-sky-700">Simpan perubahan</button></form></div></div>
@endsection
