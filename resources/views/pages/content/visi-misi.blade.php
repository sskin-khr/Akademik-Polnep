@extends('layouts.dashboard')
@section('title', 'Kelola Visi & Misi')
@section('content')
<div class="min-h-screen bg-[#375766] p-4 sm:p-8"><div class="mx-auto max-w-4xl rounded-xl bg-white p-6 sm:p-8"><a href="{{ route('dashboard') }}" class="text-sm font-semibold text-sky-700">&larr; Kembali ke dashboard</a><h1 class="mt-5 font-outfit text-2xl font-extrabold text-slate-900">Konten Visi &amp; Misi</h1><p class="mt-2 text-sm text-slate-500">Perbarui teks dan foto visi/misi.</p>@if (session('status'))<div class="mt-5 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('konten.visi-misi.update') }}" enctype="multipart/form-data" class="mt-7 space-y-6">@csrf@foreach ($items as $item)<div class="rounded-lg border border-slate-200 p-5"><p class="mb-4 text-xs font-bold uppercase tracking-wider text-sky-700">{{ $item->section_type }}</p><input name="items[{{ $item->id }}][title]" value="{{ $item->title }}" required class="mb-3 w-full rounded-lg border-slate-300 text-sm"><textarea name="items[{{ $item->id }}][description]" rows="4" required class="w-full rounded-lg border-slate-300 text-sm">{{ $item->description }}</textarea>@if ($item->image_url)<img src="{{ asset($item->image_url) }}" alt="{{ $item->title }}" class="mt-3 h-28 w-48 rounded-lg object-cover">@endif<input type="file" name="items[{{ $item->id }}][image]" accept="image/jpeg,image/png,image/webp" class="mt-3 block w-full rounded-lg border border-slate-300 p-2 text-sm"></div>@endforeach<button class="rounded-lg bg-sky-600 px-5 py-3 text-sm font-bold text-white hover:bg-sky-700">Simpan perubahan</button></form></div></div>
@endsection
