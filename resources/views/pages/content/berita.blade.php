@extends('layouts.dashboard')
@section('title', 'Kelola Berita')
@section('content')
<div class="min-h-screen bg-[#375766] p-4 sm:p-8"><div class="mx-auto max-w-5xl rounded-xl bg-white p-6 sm:p-8"><a href="{{ route('dashboard') }}" class="text-sm font-semibold text-sky-700">&larr; Kembali ke dashboard</a><h1 class="mt-5 font-outfit text-2xl font-extrabold text-slate-900">Konten Berita</h1><p class="mt-2 text-sm text-slate-500">Tambahkan berita baru beserta foto thumbnail.</p>@if (session('status'))<div class="mt-5 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('konten.berita.update') }}" enctype="multipart/form-data" class="mt-7 grid gap-5 md:grid-cols-2">@csrf<div class="md:col-span-2"><label class="text-sm font-semibold text-slate-700">Judul berita</label><input name="title" required class="mt-2 w-full rounded-lg border-slate-300 text-sm"></div><div><label class="text-sm font-semibold text-slate-700">Kategori</label><select name="category_id" required class="mt-2 w-full rounded-lg border-slate-300 text-sm">@foreach ($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></div><div><label class="text-sm font-semibold text-slate-700">Status</label><select name="status" required class="mt-2 w-full rounded-lg border-slate-300 text-sm"><option value="draft">Draf</option><option value="published">Terbit</option></select></div><div class="md:col-span-2"><label class="text-sm font-semibold text-slate-700">Foto thumbnail</label><input type="file" name="thumbnail" accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full rounded-lg border border-slate-300 p-2 text-sm"></div><div class="md:col-span-2"><label class="text-sm font-semibold text-slate-700">Ringkasan</label><textarea name="excerpt" rows="3" required class="mt-2 w-full rounded-lg border-slate-300 text-sm"></textarea></div><div class="md:col-span-2"><label class="text-sm font-semibold text-slate-700">Isi berita</label><textarea name="content" rows="7" required class="mt-2 w-full rounded-lg border-slate-300 text-sm"></textarea></div><div class="md:col-span-2"><button class="rounded-lg bg-sky-600 px-5 py-3 text-sm font-bold text-white hover:bg-sky-700">Tambah berita</button></div></form><div class="mt-10 border-t border-slate-200 pt-6"><h2 class="font-outfit text-lg font-bold text-slate-900">Berita tersimpan</h2><div class="mt-4 divide-y divide-slate-100">@forelse ($posts as $post)<div class="flex items-center justify-between gap-4 py-3"><span class="truncate text-sm text-slate-700">{{ $post->title }}</span><span class="text-xs font-semibold text-slate-500">{{ ucfirst($post->status) }}</span></div>@empty<p class="py-4 text-sm text-slate-500">Belum ada berita.</p>@endforelse</div></div></div></div>
@endsection
